{% extends 'page.html.twig' %}

{% block title %}{{ user.username }}{% endblock %}

{% block main %}
    <div><h3>Informacion del usuario {{ user.username|capitalize }}</h3></div>

    {{ form(form) }}

    <div class="btn btn-secondary mt-1 mb-1" ><a href="{{ path('user_password_change') }}" style="color: whitesmoke;">Cambiar contraseña</a></div>

{% endblock %}
