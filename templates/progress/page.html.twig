{% extends 'base.html.twig' %}

{% block body %}

    {{ include('menu--main.html.twig') }}

    <div id="react-app" class="mt-4 mb-4"></div>

    <div class="container">
        <div class="row">
            <div class="col">{% block main %}{% endblock %}</div>
        </div>
    </div>

{% endblock %}